{
  "name": "vue-cli-plugin-jxchenvip",
  "version": "1.0.4",
  "description": "基于vue-cli生成的项目注入一些命令，及eslint...配合jenkins自动化布署不同分支到不同环境",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "配合jenkins",
    "布署分支"
  ],
  "author": "jxchenvip",
  "license": "ISC",
  "devDependencies": {
    "chalk": "^2.4.2"
  }
}
